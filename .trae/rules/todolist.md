# Todolist

## 本次目标

- 修复建房页自动推荐板子在部分人数/模式组合下退回错误人数的问题。
- 为建房页补充自动推荐支持范围与通用板回退提示，减少误解。
- 继续推进昨天未完成的电子法官优化，并关注管理员权限链路是否恢复正常。

## 当前状态

- 压缩包已恢复出 `app/` 项目目录。
- 已补齐 Pages 工作流、仓库忽略规则与规则文件。
- 已完成移动端 `dvh` 与底部安全区适配。
- 已拿到新的 Supabase 项目 URL、anon key 和原有表结构信息。
- 已完成 Supabase 客户端接入、会话恢复逻辑和误退出后原位置恢复方案的前端改造。
- 已生成 `supabase/schema.sql`，包含建表、索引、触发器、RLS、RPC 与 Realtime 初始化脚本。
- 已完成 `npx tsc -b`、`npx vite build` 与 `npx eslint@9 .` 验证。
- 已确认 Supabase 表已创建，但 PostgREST 还未刷新函数缓存，已在 SQL 脚本末尾补上 `notify pgrst, 'reload schema';`。
- 已确认 RPC 已进入缓存，但 `app_generate_token()` 依赖的 `gen_random_bytes()` 在当前库中不可用，已改为纯 SQL 生成 token。
- 已确认 `app_token_hash()` 依赖的 `digest()` 在当前库中也不可用，已改为直接使用 `md5()`。
- 已在真实 Supabase 上跑通建房、加入、房主补位、发牌、夜晚记录、误退出后回原座位、结算结果同步。
- 已重新安装依赖并完成最新一轮 `npm run build`、`npm run lint` 验证。
- 已定位 GitHub Actions 发布失败根因为仓库 Pages 站点未启用，正在切换为构建后直接发布 `gh-pages` 分支的方案。
- 已确认正式线上地址可访问，但当前 `gh-pages` 上的生产包过大，正在通过关闭生产调试插件并拆分静态图片资源来修复线上首屏挂载卡顿。
- 已完成管理员后台第一版前端实现：首页新增建议信箱入口，新增管理员登录页、基础统计看板与建议信箱处理页。
- 已补充管理员后台增量 SQL 文件 `supabase/admin_dashboard.sql`，包含管理员表、设备统计、建议信箱、后台 RPC 与权限配置。
- 已确认管理员相关 SQL 在 Supabase 中执行成功，当前进入前端联调、部署和线上验收阶段。
- 已将管理员后台与建议信箱前端源码同步到远端 `main`，并在正式 Pages 地址验证首页新增入口、建议信箱提交流程和管理员登录页展示正常。
- 已完成首页第一轮视觉升级设计与“版权与免责声明”独立页，本地构建验证通过，等待新的 Pages 生产包生效。
- 已定位电子法官在微信内无声的核心原因是当前代码对微信浏览器直接禁用了语音播报，已在本地开始改成“先尝试解锁音频并播报，失败再降级”的方案。
- 已收到管理员测试账号用于后续后台验收；出于隐私安全考虑，不在仓库、规则文件或代码中记录密码明文。
- 已按要求将首页收回到更简洁的玩家端布局，保留建议信箱、版权与免责、管理员后台等新增入口，并已撤回狼人头像方案，回到最开始的首页头部风格。
- 已确认管理员后台原本仍是 Supabase 邮箱登录链路，现已在前端增加 `xpf` / `admin` 到 `@local.com` 邮箱的短账号别名兼容。
- 已根据最新确认，将管理员短账号映射更新为 `xpf@office.local`、`admin@office.local`，继续保留 `xpf` / `admin` 的登录输入方式。
- 已修正电子法官白天流程顺序：首日开启警长时，先进行警长竞选，竞选结束后再公布昨夜情况与遗言。
- 已确认 `admin_dashboard.sql` 只负责把现有 `auth.users` 账号授予后台权限，不会自动创建 Auth 用户或设置密码。
- 已按最新反馈完成首页头部回退，并删除管理员登录页中的“支持输入 xpf / admin”提示文案。
- 已确认当前“没有管理员权限”提示比 `Invalid login credentials` 更靠后一步，说明需要重点核对 `public.admin_users` 授权记录是否已按 `@office.local` 邮箱重新写入。
- 已收到新的建房反馈：当前部分人数（如 13 人）在自动推荐下会看起来像失效，需要切到自定义，准备修正推荐逻辑并补充说明文案。
- 已修复建房页自动推荐逻辑：优先使用同人数板子，并在当前模式缺少独立预设时提示“自动改用同人数通用板”。
- 已修正 13 到 20 人预设板子少 1 人的历史配置错误，并确认 13 人场景本地页面自测恢复正常。
- 已开始重构管理员后台：统计与建议信箱分区展示，建议信箱支持已读筛选、批量已读、单条删除与批量删除；同时已在 `admin_dashboard.sql` 中补齐对应 RPC。
- 已继续推进电子法官语音稳定性：房主等待页新增“解锁并测试语音”“仅解锁音频”入口，减少微信/移动端无声概率。
- 已重新完成本地 `npm run build`、`npm run lint`、诊断检查和页面自测，确认建房页 13 人自动推荐与屠城回退提示都已正常显示。
- 已复测管理员登录链路，当前输入 `xpf` 仍提示 `Invalid login credentials`，说明还需要在 Supabase Auth 中继续核对真实账号或密码。
- 用户已确认管理员账号密码无误，并已可正常进入管理员后台。
- 已继续补齐电子法官主链路：房间快照和前端类型现已带上 `enable_auto_judge`、`game_state`、`night_actions`，房主开启后会把电子法官状态同步到房间，玩家端可随房间状态自动进入对应界面。
- 已补上房主参与式主持和跨回合同步问题：房主本人若轮到夜间操作可直接在主持界面提交，本地“进入黑夜”也会按正确回合数播报和同步。
- 已再次通过最新一轮 `npm run build`、`npm run lint` 与诊断检查。

## 待办事项

- 修复自动推荐板子在部分人数/模式下的人数不匹配问题。
- 为建房页补充自动推荐支持范围与回退提示，并同步更新说明页文案。
- 重新执行最新版 `supabase/admin_dashboard.sql`，启用已读/删除与批量处理 RPC。
- 继续完善管理员后台统计图表、建议信箱批量操作体验和电子法官后续能力。
- 重新执行 `npm run build`、`npm run lint` 并检查建房页、管理员后台、电子法官相关文件状态。
- 推送本轮本地改动到 `main` 并等待 `gh-pages` 重新部署。
- 在正式线上地址验证 13 人等边界人数下的自动推荐、管理员后台与电子法官入口。
- 继续用隔离会话做多玩家联机验收，并确认管理员能力未影响原有发牌主链路。
- 等待首页视觉升级和法务页的新生产包完成部署，并在正式地址验收新版首页与“版权与免责”入口。
- 继续完善电子法官：主持词细化、夜晚结果只给房主看、真实多端同步验收，以及微信内稳定语音播报。
- 明天使用已提供的管理员测试账号验证后台登录与统计接口，但敏感凭据仅在当前会话上下文中使用，不写入项目文件。
