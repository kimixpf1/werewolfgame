# Todolist

## 本次目标

- 恢复压缩包项目源码并完成仓库初始化。
- 补齐 GitHub Pages 自动部署配置。
- 检查并优化手机端微信内使用体验。
- 完成构建、检查与模拟验证后推送到 `kimixpf1/werewolfgame`。

## 当前状态

- 压缩包已恢复出 `app/` 项目目录。
- 已补齐 Pages 工作流、仓库忽略规则与规则文件。
- 已完成移动端 `dvh` 与底部安全区适配。
- 已将失效的 Supabase 依赖改造成匿名远程联机存储层，创建房间的 `failed to fetch` 问题已进入新方案验证阶段。

## 待办事项

- 再次核对联机链路的创建房间、加入房间、发牌与房间内同步逻辑。
- 再次执行构建、lint 与类型检查。
- 推送仓库并确认 Pages 可访问地址。
