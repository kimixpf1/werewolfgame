# 项目迭代记录

## 2026-04-02

### 本次目标

- 将管理员后台从同页分区重构为两个单独页面：玩家统计页与建议信箱页。
- 调整后台入口、hash 路由与返回逻辑，保证管理员登录后进入独立页面而不是标签切换。
- 完成改造后重新构建、发布并核对正式线上页面结构。

### 当前状态

- 已确认项目为 Vite + React + TypeScript + Tailwind。
- 已确认核心能力包括创建房间、加入房间、联机同步、发牌、法官记录、电子法官、角色说明与历史记录。
- 已读取参考规则并在当前项目建立长期规则文件。
- 已定位原网页 `failed to fetch` 的核心风险为原 Supabase 域名不可解析，并已改造为新的匿名远程联机后端方案。
- 已补充 GitHub Pages 自动部署工作流与移动端安全区适配。
- 已收到新的 Supabase 项目地址、anon key 以及原始 `rooms`、`players` 表结构说明。
- 已确定当前版本走“版本 1：轻量安全版”，重点修复误退出后回房不新增占位。
- 已完成 Supabase 前端接入改造，新增房主/玩家 token、会话恢复、昵称认领原座位与离线状态心跳逻辑。
- 已生成 `supabase/schema.sql`，用于在 Supabase 中一次性创建表结构、RLS、RPC 和 Realtime 配置。
- 已通过 `npx tsc -b`、`npx vite build` 与 `npx eslint@9 .` 的本地检查。
- 已确认 SQL 执行后表结构生效，但 PostgREST 函数缓存尚未刷新，因此在脚本末尾补充了 `notify pgrst, 'reload schema';`。
- 已确认 RPC 缓存刷新后开始可调用，但 `app_generate_token()` 依赖的 `gen_random_bytes()` 在当前库中不可用，已改成纯 SQL 生成 token。
- 已确认 `app_token_hash()` 依赖的 `digest()` 在当前库中也不可用，已改成直接使用 `md5()`。
- 已用真实 Supabase 和双页面隔离上下文跑通建房、加入、房主补位、发牌、夜晚记录、误退出恢复原座位与结算弹窗同步。
- 已重新安装依赖并通过最新一轮 `npm run build`、`npm run lint` 检查。
- 已定位 GitHub Pages 工作流失败原因为仓库尚未启用 Pages 站点，当前改为通过 Actions 构建后直接发布 `gh-pages` 分支。
- 已确认正式线上地址可访问，但当前 `gh-pages` 生产包体积异常偏大，正在通过关闭生产调试插件和拆分静态图片资源修复线上空白挂载问题。
- 已完成管理员后台第一版实现，包含首页建议信箱入口、管理员登录、基础数据统计和建议信箱处理页。
- 已新增 `supabase/admin_dashboard.sql` 增量脚本，覆盖管理员账号映射、设备统计埋点、建议提交和后台查询 RPC。
- 已确认管理员相关 SQL 在 Supabase 中执行成功，当前开始收尾前端联调、部署与线上验证。
- 已通过 GitHub API 方式将管理员后台与建议信箱前端源码补推到远端 `main`，并在正式 Pages 地址完成入口与建议提交验收。
- 已完成首页第一轮暗夜风格升级，并新增“版权与免责声明”独立页面与首页入口；本地构建和页面自检已通过。
- 已定位电子法官在微信内无声的根因是当前代码对微信浏览器直接禁用语音播报，现已在本地切到“优先解锁音频并尝试播报，失败再降级”的方案继续迭代。
- 已收到管理员后台测试账号供后续验收使用；为保护隐私与安全，未将密码写入仓库、规则文件或代码。
- 已按用户要求把首页退回更简洁的玩家端布局，保留建议信箱、版权与免责、管理员后台等入口，并将首页主视觉改为狼人头像方案待线上确认。
- 已在前端增加 `xpf` / `admin` 的短账号别名兼容，但同时确认当前线上输入 `admin@local.com + kimiclaw1` 仍返回 `Invalid login credentials`，说明问题不只在输入形式。
- 已根据用户最新确认，把前端短账号别名与仓库 SQL 占位统一更新为 `xpf@office.local`、`admin@office.local`。
- 已确认 `supabase/admin_dashboard.sql` 仅负责为已存在的 `auth.users` 账号授予后台权限，不会自动创建管理员 Auth 用户或设置密码。
- 已修正电子法官白天流程为“首日先警长竞选，竞选结束后再公布昨夜情况与遗言”。
- 已收到最新界面反馈：当前狼人头像版首页违和，需要回退到最开始首页头部；管理员登录页中“支持输入 xpf / admin”提示也需要删除。
- 已确认当前管理员错误已从 `Invalid login credentials` 进入“没有管理员权限”阶段，说明 Auth 登录大概率已成功，接下来要重点核对 `public.admin_users` 是否已按 `xpf@office.local`、`admin@office.local` 正确授权。
- 已完成首页头部回退，恢复为最开始的简洁版图标样式，同时保留建议信箱、管理员后台、版权与免责入口。
- 已删除管理员登录页中“支持输入 xpf / admin”的可见提示；本地 `npm run build`、`npm run lint` 与浏览器页面自测已通过。
- 已收到新的建房反馈：部分人数在自动推荐下会退回错误人数板子，看起来像只能切到自定义，当前准备修正推荐算法并补充清晰提示。
- 已修复建房页自动推荐逻辑：自动推荐改为优先使用同人数板子，当前模式缺少独立预设时会明确提示“已改用同人数通用板”。
- 已额外发现并修复 13 到 20 人预设板子少 1 人的历史配置错误；本地构建、lint 和 13 人页面自测已通过。
- 已开始重构管理员后台：将统计与建议信箱分区，补充图表化统计视图，并为建议信箱增加已读筛选、批量已读、单条删除和批量删除所需的前后端能力。
- 已更新 `supabase/admin_dashboard.sql`，新增建议信箱 `is_read` / `read_at` 字段、批量已读 RPC、单条删除 RPC、批量删除 RPC 与更细化的统计摘要字段。
- 已继续推进电子法官语音稳定性：房主等待页新增“解锁并测试语音”“仅解锁音频”按钮，便于微信和移动端开局前先验证语音。
- 已重新执行本地 `npm run build`、`npm run lint` 与诊断检查，当前前端无新增报错。
- 已在本地预览环境完成真人模拟：13 人屠边显示 `13/13` 正常，13 人屠城会明确提示“改用同人数通用板”。
- 已在本地继续验证管理员登录，输入 `xpf` 仍返回 `Invalid login credentials`，说明当前问题仍在 Supabase Auth 用户或密码本身，不在前端短账号映射。
- 用户已在 Supabase 后台核对管理员账号密码，并确认现在已经可以正常进入管理员后台。
- 已继续完善电子法官同步链路：房间快照补齐 `enable_auto_judge`、`game_state`、`night_actions` 字段，电子法官改为通过 `game_state` 同步细环节，不再把 `seer`、`witch` 等细步骤直接写入受限的 `current_phase`。
- 已修正电子法官的多人联动缺口：房主开启后会把状态写回房间，玩家端可跟随房间同步状态自动进入等待/发牌确认/夜晚流程，而不是只在房主本地生效。
- 已补上房主参与式主持：如果房主本人也属于当前夜间行动角色，会在主持面板中直接看到自己的操作区并提交，不再因为主持身份而无法完成本角色操作。
- 已修正电子法官跨回合问题：进入下一夜时会按正确回合数播报与同步，不再因为 `setState` 异步导致第二夜仍沿用上一夜回合号。
- 已再次通过最新一轮本地 `npm run build`、`npm run lint` 与诊断检查。
- 已确认管理员后台当前实现仍是单一 `AdminDashboardSection` 内部通过 `activeTab` 在“玩家统计 / 建议信箱”之间切换，不符合用户要求的两个单独页面；这次问题的根因是实现方案本身，而不只是线上没推送到最新包。
- 已将管理员后台重构为 `AdminStatsSection` 和 `AdminFeedbackSection` 两个独立页面，并在 `App.tsx` 中新增 `admin-stats` / `admin-feedback` 视图与 `#admin/stats` / `#admin/feedback` hash 路由。
- 已删除旧的 `AdminDashboardSection` 单页标签实现，本地静态预览中已完成真实登录点击验证：登录后进入“玩家统计中心”，再跳转到“建议信箱”页面，确认两者已不再处于同一页。
- 已补充 `admin_list_feedback` 旧签名兼容回退，解决 Supabase 端 schema cache 尚未刷新到 `p_is_read` 参数时信箱列表报错的问题；旧环境下阅读筛选改由前端兜底。
- 已重新强制构建 `app/dist`，并确认产物中已包含“玩家统计中心”“进入建议信箱”“返回玩家统计”等新页面文案。

### 下一步

- 推送本轮后台拆页改动并等待新的 `gh-pages` 生产包完成部署。
- 在正式线上地址验证管理员后台是否已变为两个单独页面，并复核建房自动推荐仍保持最新版本。
- 如 Supabase 端仍未完整刷新 `admin_list_feedback(text, boolean)` 新签名，重新执行最新版 `supabase/admin_dashboard.sql` 并触发 schema reload。
- 在正式线上地址继续做多玩家全链路联机验收，并确认管理员功能未影响原有联机流程。
- 等待首页视觉升级与法务页的新生产包完成部署，并在正式地址继续验收。
- 继续完善电子法官的主持词流程、夜晚结果只给房主看与微信内语音稳定性，并在真实多端场景补做多人同步验收。
- 明天使用已提供的管理员测试账号继续做后台登录与统计验收，但敏感凭据仅在会话中使用，不落盘保存。
